name: Backend Docker CI

on:
  push:
    paths: ['backend/**']
  workflow_dispatch:

jobs:
  build-docker:
    name: 🐳 Đóng gói Docker Image
    runs-on: ubuntu-latest
    
    steps:
      - name: Lấy code về Runner
        uses: actions/checkout@v4

      - name: Kiểm tra phiên bản Docker trên máy GitHub
        run: docker --version

      - name: Build Docker Image
        run: |
          cd backend
          docker build -t app-backend:v1 .
          
      - name: Xem danh sách Image đã tạo
        run: docker images